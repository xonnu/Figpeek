{
  "name": "chrono-svelte-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "config": {
    "dist": "dist",
    "domain": "chrono-svelte-template"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && surge ./%npm_package_config_dist% --domain %npm_package_config_domain%.surge.sh"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.0-next.30",
    "autoprefixer": "^10.4.2",
    "postcss": "^8.4.6",
    "surge": "^0.23.1",
    "svelte": "^3.44.0",
    "tailwindcss": "^3.0.23",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "svelte-spa-router": "^3.2.0"
  }
}
